{
  "name": "deadlock-lit-actions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "upload": "node upload.mjs",
    "build:upload": "npm run build && npm run upload"
  },
  "dependencies": {
    "@lit-protocol/lit-node-client": "^6.0.0",
    "@storacha/client": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
